# txt_editor
------------------
Text editor, study project. Uses ```ncurses-dev```.

## Build

```
make
```
## Installation 

```sudo cp run /usr/bin/txt_editor``` (optional) 

## Usage

``` ./run filename ```
